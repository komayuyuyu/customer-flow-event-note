# イベント候補データ形式

イベント情報を更新するときは、候補を `data/candidates.json` に書き、`scripts/evaluate_candidates.py` で影響度・確からしさ・影響時間帯を計算する。

週1回の広めチェックでは、毎週日曜日の夜に、実行日を基準に当日から60日先までを候補確認範囲にする。毎朝チェックでは、当日・翌日・翌々日を日付別に確認し、直近の更新・中止・延期・放送変更も確認する。

客足注意イベントはこのWebアプリ内で確認する。Google Calendarへの `[客足注意]` 登録は行わない。

`data/candidates.json` は調査した入力データ、`data/events.json` は表示用に計算済みの出力データ。`data/events.json` には過去に直接追加したイベントや履歴も残るため、`data/candidates.json` と完全一致しなくてもよい。更新時は既存イベントを削除せず、`scripts/evaluate_candidates.py` で候補をマージする。

```json
[
  {
    "title": "イベント名",
    "startAt": "2026-07-05T21:00:00+09:00",
    "endAt": "2026-07-05T23:00:00+09:00",
    "broadcast": "地上波・無料配信",
    "category": "スポーツ中継",
    "area": "全国",
    "liveReason": "決勝戦のためリアルタイム視聴の動機が強い",
    "nationalReach": 3,
    "liveUrgency": 3,
    "accessibility": 3,
    "buzz": 3,
    "significance": 3,
    "officialConfirmed": true,
    "sources": [
      {"title": "公式日程", "url": "https://example.com/official"},
      {"title": "報道", "url": "https://example.com/news"}
    ]
  }
]
```

## 追加できる表示用フィールド

- `category`: `スポーツ中継`、`花火`、`祭り`、`パレード`、`イルミネーション`、`セール関連` など。
- `area`: `兵庫`、`大阪`、`京都`、`東京`、`全国` など。
- `trafficReason`: 画面に「メモ」として表示する情報。閲覧者が判断に使える交通規制、来場集中、帰宅混雑などを端的に書く。「候補扱い」「過大評価しない」「表示上は」「集客記録では」など制作・運用側の説明は入れない。
- `internalNote`: 画面に表示しない制作・運用メモ。候補の更新条件、評価の判断理由、`recordLink` の設定理由などを書く。
- `predictedWindows[].reason` は使用しない。予測時間帯はラベルと時刻だけにし、交通規制や通常と異なる人流などの固有情報は `trafficReason` にまとめる。
- `視聴準備・早めの帰宅` は時間帯ごとカードに表示しない。
- `リアルタイム視聴` は時間帯ラベルと時刻をカードに表示し、「生中継・ライブ配信をその時間に見る人が増える可能性」という自明な理由文だけを表示しない。
- `来場・交通混雑` と `終了後の帰宅混雑` は、現地イベントなら当然のため時間帯ごとカードに表示しない。
- `inPersonEvent`: `true` にすると、`イベント開催中` のラベルと時刻だけを表示する。
- `impactLevelOverride`: 神戸の勤務先への影響度を手動で調整する。東京など遠方の現地イベントは、テレビ放送・全国的な話題化が見込める場合だけ `大` にする。
- `championship`: ヴィッセル神戸など優勝接近イベント用の詳細。

## 勤務カレンダー由来イベント

勤務カレンダー・シフト表の「イベント関連」欄から入れる店内イベントは `data/store-events.json` に書く。

- 勤務カレンダーは出典として `sources` に保持し、`broadcast` や表示文には入れない。
- 「勤務カレンダーのイベント関連欄に記載」といった制作側の出典説明は表示せず、閲覧者に必要なイベント内容だけを書く。
- セールでは「来館数や客層、店頭の流れに影響する」といった共通認識を `trafficReason` に書かず、固有の追加情報がある時だけ設定する。
- 長期イベントは `showEachDay: true` を付け、`startAt` と `endAt` に実際の開始日・終了日を入れる。
- `predictedWindows[0].date` は開始日と合わせる。
- 表示はしたいが通常日扱いで保存したいイベントは `recordLink: false` を付ける。現在の固定対象は `棚卸`。
- `recordLink: false` のイベントは、EVENT SIGNAL と週間予定には表示されるが、集客記録の `relatedEvents` / `eventIds` には入らない。
- `レイアウトUP`、`館店長会`、`施策〆`、`OL店長会`、`HELP ○○` は掲載不要。

```json
{
  "championship": {
    "team": "ヴィッセル神戸",
    "winsToTitle": 2,
    "condition": "ヴィッセルが2連勝し、2位が次節で勝点を落とすと優勝決定",
    "runnerUpCondition": "2位が勝利し、得失点差を6点以上詰めた場合のみ逆転"
  }
}
```

## 0〜3の判定軸

- `nationalReach`: 全国・年代横断の認知度
- `liveUrgency`: その時刻に見る必要性
- `accessibility`: 地上波・無料配信などの見やすさ
- `buzz`: 公開SNS・ニュースでの話題性
- `significance`: 決勝、最終公演、代表戦など一回性の強さ

店舗のお客様層は判定に使わない。

## 表記ルール

- サッカーのワールドカップは `サッカーW杯` と表記する。
- 女子バレーボールのVNLは `VNL女子` と表記する。
- ヴィッセル神戸の優勝接近時は、タイトルの先頭を `ヴィッセル神戸` に統一する。
- 対戦国名は半角英大文字3文字の国コードで表記する（例: `JPN対BRA`、`JPN対THA`）。
- スポーツの進行段階を示すラウンドは、大文字 `R` と数字で表記する（例: `R32`、`R16`）。
- 同じ大会の全イベントへ、完全に同じイベント名を必ず付ける。
- 各行を単独で読んでも、何の競技・大会か分かる状態にする。

## 絶対ルール

- 週1回の広めチェックでは、毎週日曜日の夜に実行日から60日先までのイベント情報を確認する。
- 客足注意イベントはGoogle Calendarに登録しない。表示・更新・記録の正本はWebアプリ側のJSONとFirestoreに寄せる。
- 既存イベントは過去分を含めて削除せず、イベントID単位で追加・更新する。
- 中止・延期になったイベントも履歴として残し、削除ではなく状態を更新する。
- 営業時間外でも除外しない。
- 夜のイベントは2〜3時間前からの帰宅準備を評価する。
- 深夜・早朝のイベントは、その日の午前〜昼の外出控えを評価する。
- 公式確認と複数情報源を区別して、確からしさを付ける。
- 花火、ルミナリエ、優勝パレード、神戸祭りのような広域人流イベントは、兵庫、大阪、京都、東京を優先して確認する。
- 花火は `data/event-source-registry.json` の公式巡回先と検索語を使う。毎朝は兵庫・大阪の当日・翌日・翌々日、週次は兵庫・大阪・京都・東京の60日分を、日付×地域単位で確認する。
- 「花火」だけでなく、「花火大会」「打上花火」「納涼」「夏まつり」「サマーカーニバル」「夜市」「夢花火」「舞花火」も検索し、名称に「花火」がない催事内花火を拾う。
- 前年情報の日付を流用せず、当年の主催者・自治体・観光協会の公式情報で開催日、時間、交通規制を確認する。
- 客足への直接影響だけでなく、年1回の球宴、代表戦、決勝、表彰・受賞、全国的な大型特番など、幅広い来店客との会話に使える話題性も収集基準にする。全国地上波、無料配信、ファン投票、大手メディアでの継続報道のいずれかがあるイベントを優先する。
- NPBオールスターゲームは毎年の固定確認対象とし、公式開催要項で両試合の日程・会場・開始時刻・放送予定を確認して、各試合を個別に登録する。
- 垂水駅周辺など勤務先の近距離で行われる祭り・地域イベントは、知名度や規模にかかわらず収集する。
- 海神社の夏祭りは毎年の固定確認対象とし、6月末から7月前半に公式サイトで海の日を含む開催期間と交通規制を確認する。近距離の直接影響として、特段の反証がなければ `影響 大` とする。
- 湊川神社の祭典・行事等一覧を毎朝・週次チェックの固定巡回先にし、祭り、露店、神輿、夜間催事など来街者が増える行事を候補化する。
- 湊川神社の夏まつり（献燈祭・菊水天神祭）は毎年の固定確認対象とし、7月中に公式サイトで8月22日〜26日の開催時間と交通・駐車場案内を確認する。
- ビショップ音楽祭は毎年の固定確認対象とし、Bshop公式サイトと音楽祭公式サイトで開催日、会場、開演時刻、出演者、交通案内を確認する。
- フジロックのような複数日イベントは `showEachDay: true` を付け、開始日から終了日までの全開催日に表示する。
- 祭りは、関西以外でも日本全国の誰もが知っている規模のものは収集対象に含める。
- 東京など遠方の現地イベントは、神戸の勤務先への直接影響を過大評価しない。テレビ放送や全国的な話題化で影響が見込める時だけ `影響 大` とする。
- ヴィッセル神戸が優勝争いに入った場合は、次戦以降の試合をイベント候補に入れ、`championship` に「あと何勝」「優勝条件」「2位以下の逆転条件」を書く。
